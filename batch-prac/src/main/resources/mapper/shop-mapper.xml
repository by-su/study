<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rootbly.batchprac.shop.mapper.ShopMapper">

    <select id="findAllOrderByTotalSalePriceDesc" resultType="com.rootbly.batchprac.shop.Shop">
        SELECT
            id,
            name,
            total_sale_price as totalSalePrice
        FROM shop
        ORDER BY total_sale_price DESC, id ASC
    </select>

    <insert id="bulkUpsertShopRankSnapshot" parameterType="java.util.List">
        MERGE INTO shop_rank_snapshot (shop_id, total_sale_price, rank)
        KEY (shop_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.shopId}, #{item.totalSalePrice}, #{item.rank})
        </foreach>
    </insert>

</mapper>